# Dockerfile per Frontend React - Development Mode
FROM node:20-alpine

# Imposta la directory di lavoro
WORKDIR /app

# Installa dipendenze
COPY package*.json ./
RUN npm install

# Copia il codice sorgente
COPY . .

# Esponi la porta Vite
EXPOSE 5173

# Comando per avviare il dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
